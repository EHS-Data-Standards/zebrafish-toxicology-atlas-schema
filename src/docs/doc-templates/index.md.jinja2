# {{ schema.name }}

## Documentation

This documentation is hosted under `/docs/`. If you are not automatically redirected,
visit [here](/docs/).

## Classes

| Class | Description |
| --- | --- |
{% for c in schemaview.all_classes(ordered_by="lexical").values()|sort(attribute='name') -%}
| [{{ c.name }}](/docs/{{ gen.link(c) }}) | {{ c.description }} |
{% endfor %}

## Slots

| Property | Description |
| --- | --- |
{% for np in schemaview.all_slots(ordered_by="lexical").values()|sort(attribute='name') -%}
| [{{ np.name }}](/docs/{{ gen.link(np) }}) | {{ np.description }} |
{% endfor %}

## Enumerations

| Enumeration | Description |
| --- | --- |
{% for e in schemaview.all_enums().values()|sort(attribute='name') -%}
| [{{ e.name }}](/docs/{{ gen.link(e) }}) | {{ e.description }} |
{% endfor %}
